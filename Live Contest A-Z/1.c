#include<stdio.h>
int main()
{
    long long int n;
    int t,m,i;
    scanf("%d",&t);
    while(t--)
    {
        int sum=0,trust=0;
        scanf("%d\n",&m);
        char a[m];
        for( i=0;i<m;i++)
        {
            scanf("%s",&a[i]);
        }
        /*for(i=0;i<m;i++)
        {
            trust=10*trust+a[i];
        }
        printf("%d",trust);
        if(sum%2==0)
        {
            if(trust%2!=0)
            {
                printf("-1\n");
            }
        }*/

    }

}
